# Начало практики по ML
##### После прочтения замечательной книги Себастьяна Рашки[«Машинное обучение с PyTorch и Scikit-Learn»](https://sebastianraschka.com/books/machine-learning-with-pytorch-and-scikit-learn/) пора закпрелять полученную теорию на практике.

Начнем с датасета ,который я выбрал для практики.Его я нашел на kaggle и честно говоря долго не искал,взял первый попавшийся на глаза .О чем в дальнейщем немного пожалел ,но об этом позже.

Датасет [9000+ Popular People in Cinema](https://www.kaggle.com/datasets/aminasalamt/9000-popular-people-in-cinema/data) как понятно из названия речь пойдет о популярных людях и их вкладе кинематограф.

Если мы зайдем в раздел Detail,то увидим такие данные в нашей таблице:
| Поле                  | Описание                                      | Значение |
| --------------------- |:---------------------------------------------:| --------:|
| File Name             | Название файла                                | `popular_people.csv` |
| Format                | Формат данных                                 | CSV |
| Size                  | Размер датасета                               | ~10,000 rows |
| Source                | Источник                                      | TMDB API |
| name                  | Публичное имя                                 | 7662 unique values |
| gender                | Кодировка пола                                | 0–3 |
| known_for_department  | Основная сфера деятельности                   | Acting 93% |
| original_name         | Оригинальное имя                              | 7665 unique values |
| popularity            | Индекс популярности                           | 0.58 – 48 |

После того,как мы выбрали интересующий нас датасет и увидели что в нем содержится,поставим для себя задачу ,которую на необходимо решить.Поставленной задачей оказалось построение алгоритма ML ,где входные признаки "known_for_department" ,а метки "gender".

Приступаем к реализации алгоритма ML.Мы скачали данные датасет и с помощью команд 
```python
df.info()
df.head()
```
Из библиотеки Pandas мы проверили ,что вся информация о данных в датасете совпадает и мы можем продолжать дальше.Теперь начнем обучение ML и выяснилось позже нужно было бы поработать с данными ,но поскольку все это только первые опыты,пусть лучше на них пройдут все баги и костыли ,чтобы в последующем такого не допускать.Хватит слов и перейдем к делу.Из библиотеки sklearn мы обратились к модулю LabelEncoder с целью кодиравония категориальных признаков.И давайте же посмотри что у нас вышло,с помощью команды 
```python
np.unique(x)
```
Из библиотеки Numpy мы видим,что у нас получилось массив из 13 вариантов ,но постойте же , в Датасете мы знаем ,что всего профессий 12 ,а не 13 как же так вышло ?.Все очень просто как я уже сказал ранее ,данные я никоем образом не подготовил и если мы с помощью Python сами преобразуем категориальные признаки ,то в результате получим массив из 12 варинтов.

Ниже представлен код 
```python
class_mapping = {label: idx for idx, label in enumerate(df['known_for_department'].dropna().unique())}
class_mapping
```

И в чем же проблема ? А проблема самая простая в таблице просто оказались значения Nan и встроенный в библиотеку Sklearn модуль просто его закодировал.А также как мы видим из когда выше ,что использовали команду 
```python
.dropna()
```
Которая удаляет Nan значения.И теперь стало интересно,а сильно ли повлияют на результат эти значения Nan или все-таки они ни на что не влиют.Постараемся узнать об это дальше.

Далее давайте взглянем на наши данные с помощью библиотеки matplotlib.И как говорилось ранее мы понимаем ,где совершили ошибку ,а именно в выборе данных.Мало того,что мы взяли всего один признак ,по которому будем строить модель классификатор ,так еще и классифицировать собираемся не в 2 значени ,а в 4 ,что тоже скажется на нашей модель.И как многие уже понимают модель окажется плохой,но для нас плохой опыт это опыт и ничего в этом нет страшного.

Ну вот теперь уже точно приступаем к обучению модели.Из Sklern возьмем необходимые для нас метрики , логистическую регрессию и модуль,деляющий наши данные на обучающие и тестовые.

```python
from sklearn.linear_model import LogisticRegression
from sklearn.multiclass import OneVsRestClassifier
from sklearn.model_selection import train_test_split
from sklearn.metrics import precision_score
from sklearn.metrics import f1_score
from sklearn.metrics import recall_score
```
После чего разделим наши данные и попытаемся их обучить и получить точность нашей модели и полноту.

```python
X_train, X_test, y_train, y_test = train_test_split(x, y, test_size=0.3, random_state=0,stratify=y)
lr=OneVsRestClassifier(LogisticRegression(l1_ratio=1,C=1.0,solver='liblinear'))
```
Но при вызове метода 
```python
fit(X_train,y_train)
```
У нас выпадает ошибка ,в чем же дело ?

Все оказалось просто данные опять оказались плохо подготовленны и sklearn не может переварить тип данных object ,который мы ему подаем и к тому же еще он хочет получить массив в виде 
```python
[
  [123],
  [213]
]
``` 
А не в виде 

```python
[
  123,
  213
]
``` 
И мы используем команды для того,что перевести данные в нужном виде 

```python
X_train=X_train.reshape(-1,1)
y_train = y_train.astype(int)
X_test=X_test.reshape(-1,1)
y_test = y_test.astype(int)
``` 
Теперь наша модель спокойно обучилась ,но что же там по результатам ,как я и говорил результат оказался неутешительным всего 52.8 ,что уже понятно слабо притендует на классификатор,скорее на угадайку.С чем это связано в принципе уже понятно ,но давайте попробуем более мощные модели ML такие как boosting ,RandomForest и зацепи еще SVM причем kernel с ядром Гауса,что они покажу нам ?

И ничего в приципе удивительно результат практически одинаковый.И разница буквально в несколько процентов ,а где-то и вовсе такой же результат.

* SVM
    * 52.8
* RandomForest
    * 53.1
* Boosting
    * 52.9

А что же там с данными без Nan сильно ли они повляли на итоговый результат ? 
* С Nan
    * 53.12
* Без Nan
    * 53.07

Как оказался не очень то и сильно ,хотя нужно брать в расчет то , что задача поставленная в начале оказался неправильной для такого датасета ,а точнее очень сложной ,поскольку слишком мало данных для классификации ,всего лишь один признак и классификация у нас еще множественная.

Как итог опыт оказался неудачным ,но свой вклад в дальнейшие навыки он вложил и точно не окажется совсем уж бесполезным.Так что не унываем и работаем дальше!



